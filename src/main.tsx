// Imports React and/or third party components
import ReactDOM from 'react-dom/client';

// Impor own components
import './index.css';
import { JournalApp } from './JournalApp';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <JournalApp />
)
